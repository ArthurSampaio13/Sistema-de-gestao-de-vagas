version: '3.9'

services:
  postgres:
    container_name: gestao_vagas_postgres
    image: postgres
    ports:
      -
    environment:
      - POSTGRES_USER=
      - POSTGRES_PASSWORD=
      - POSTGRES_DB=